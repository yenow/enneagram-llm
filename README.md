# Enneagram LLM Project

This project is a FastAPI application for an Enneagram-based LLM.

## Setup

1.  Install dependencies:
    ```bash
    pip install -r requirements.txt
    ```

2.  Run the application:
    ```bash
    uvicorn main:app --reload
    ```
